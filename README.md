# Dynamic-Path-Finding-Searches
This project implements a grid-based pathfinding system using two intelligent search algorithms: A* (A-Star) and Greedy Best First Search (GBFS). The system is developed using Python and Tkinter and allows the user to interactively create walls, set start and goal positions, enable dynamic obstacles, and visualize how algorithm searches for a path.
